let number = 10;
let result = (number % 2 == 0) ? "Even" : "Odd";
console.log(result);

let value = 42;
let type = (typeof value === 'number') ? 'Number' : 'Not a Number';
console.log(type); // Output will be "Number" since value is a number